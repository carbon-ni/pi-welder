{
  "name": "pi-welder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pi extension that registers harness-level tool failures",
  "scripts": {
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\"",
    "lint": "tsc --noEmit",
    "check": "npm run lint && npm test"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  }
}
