{
  "name": "pi-welder-release-consumer",
  "private": true,
  "version": "1.0.0",
  "description": "Pinned peer contract for scripts/verify-package.mjs. The script links these exact versions from the repository install, so verification runs offline.",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "typebox": "1.3.7"
  }
}
